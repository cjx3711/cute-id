# cute-id

A Rust library for generating cute, readable IDs by combining random words from different categories (animals, colors, adjectives, and adverbs).

## Features

- Generate human-readable, memorable IDs
- Customizable format with different word combinations
- Customizable separator between words
- Default format: `adjective-color-animal` (e.g., `happy-blue-cat`)

## Installation

Add this to your `Cargo.toml`:

```toml
[dependencies]
cute_id = "0.1.0"
```

## Usage

### Default Generator

The default generator creates IDs in the format `adjective-color-animal`:

```rust
use cute_id::CuteIdGenerator;

let mut generator = CuteIdGenerator::default();
let id = generator.generate();
// Example: "passionate-eggplant-wombat"
```

### Custom Generator

You can customize the format by specifying which parts to include and what separator to use:

```rust
use cute_id::{CuteIdGenerator, IdPart};

let mut generator = CuteIdGenerator::new(
    vec![IdPart::Adverb, IdPart::Adjective, IdPart::Animal],
    "_"
);
let id = generator.generate();
// Example: "frankly_superb_swan"
```

## Available Parts

- `IdPart::Animal` - Animal names (e.g., "cat", "dragon", "penguin") - 171 options
- `IdPart::Color` - Color names (e.g., "blue", "emerald", "crimson") - 85 options
- `IdPart::Adjective` - Adjectives (e.g., "happy", "brilliant", "majestic") - 851 options
- `IdPart::Adverb` - Adverbs (e.g., "quickly", "gracefully", "boldly") - 328 options

## Unique ID Capacity

The number of unique IDs you can generate depends on the format you choose:

- **Default format** (`adjective-color-animal`): **12,369,285** unique combinations
- **Custom formats**: The total combinations depend on which parts you include. For example:
  - `adverb-adjective-animal`: **47,730,888** unique combinations
  - `adjective-animal`: **145,521** unique combinations
  - `color-animal`: **14,535** unique combinations

You can calculate the total for any custom format by multiplying the number of options for each part you include.

## Example IDs

Here are some example IDs generated by the library:

**Default format (adjective-color-animal):**

- `beauteous-pumpkin-owl`
- `calm-eggplant-leviathan`
- `precise-aquamarine-axolotl`
- `illustrious-lime-hedgehog`
- `hilarious-mustard-chipmunk`

**Custom format (adverb_adjective_animal):**

- `frankly_superb_swan`
- `innocently_ready_parrot`
- `quarrelsomely_desirous_tiger`
- `fortunately_comprehensive_skunk`

## License

MIT License - see [LICENSE](LICENSE) file for details.
